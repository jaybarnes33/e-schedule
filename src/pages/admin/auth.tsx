import AuthWrapper from "@/components/AuthWrapper"

const Auth = () => {
  return (
    <AuthWrapper
      id="admin-auth"
      heading="Admin Sign Up"
      description="Sign up with your google account to gain admin privileges"
    />
  )
}

export default Auth
